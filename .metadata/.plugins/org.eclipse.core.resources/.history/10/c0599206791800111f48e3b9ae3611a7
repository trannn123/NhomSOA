package service;
import dao.UserDAO;
import model.User;

public class ChungThucImpl implements IChungThucService{
	UserDAO userDAO = new UserDAO();

    @Override
    public User login(String username, String password) {

        return userDAO.login(username, password);

    }
}
